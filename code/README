This folder contains all code (mostly in R, some shell scripts...)

misc/                   -- code for trying different ideas, not necessarily ODNB stuff
network_visualization/  -- code that produces static visualizations of networks
ODNB/                   -- code that processes the ODNB code & the model fitting => confidence matrix
other_data_sources/     -- code that processes other data sources (doesn't go very far)
